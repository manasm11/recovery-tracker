# Base URL of the backend API. Leave empty in development (Vite proxies /api
# to the local backend). In production set this to the deployed backend URL,
# e.g. https://recovery-tracker-api.fly.dev
VITE_API_URL=
